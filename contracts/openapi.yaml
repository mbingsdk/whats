openapi: 3.1.0
info:
  title: WABA Control identity Meta ingestion and guarded Inbox API
  version: 0.4.0
  description: >-
    Implemented identity, read-only Meta assets, signed ingestion, scoped Inbox and guarded text intents.
    Paid/template sends remain unauthorized. Cookie mutations require matching Origin, CSRF cookie/header and the session-bound CSRF digest.
    Organization IDs never establish authorization.
  license:
    name: Proprietary
    url: https://choosealicense.com/no-permission/
servers:
  - url: /
security: []
tags:
  - name: Foundation
    description: Local process and required database health.
  - name: Identity
    description: Global company identity and own-session security.
  - name: Organization
    description: Membership-checked, permission-scoped organization operations.
  - name: Meta
    description: Existing assets and authenticated webhook ingestion.
  - name: Inbox
    description: Scoped conversations, reviewed pricing and controlled text intents.
paths:
  /api/v1/inbox:
    get:
      operationId: "inboxGETinbox"
      summary: "Load scoped Inbox workspace"
      tags: ["Inbox"]
      security: [{"SessionCookie": []}]
      parameters: [{"$ref": "#/components/parameters/RequestID"}]
      x-permission: "inbox.view"
      description: "Session-derived organization and current resource scope are checked on every call. Writes require verified email, matching Origin and session-bound CSRF. Registry, budget creation, note redaction and sending settings require recent reauthentication and confirmed MFA. Paid/template authority remains CLOSED."
      responses: {"200": {"description": "Scoped operation result; a queued or accepted intent is not delivery proof.", "content": {"application/json": {"schema": {"$ref": "#/components/schemas/InboxSetup"}}}}, "403": {"$ref": "#/components/responses/Error"}, "409": {"$ref": "#/components/responses/Error"}, "default": {"$ref": "#/components/responses/Error"}}
  /api/v1/conversations:
    get:
      operationId: "inboxGETconversations"
      summary: "Search authorized conversations"
      tags: ["Inbox"]
      security: [{"SessionCookie": []}]
      parameters: [{"$ref": "#/components/parameters/RequestID"}, {"name": "q", "in": "query", "schema": {"type": "string"}, "description": "Scope is applied before search and pagination."}, {"name": "status", "in": "query", "schema": {"type": "string"}, "description": "Scope is applied before search and pagination."}, {"name": "cursor", "in": "query", "schema": {"type": "string"}, "description": "Scope is applied before search and pagination."}, {"name": "priority", "in": "query", "schema": {"type": "string"}, "description": "Optional narrowing within current authorized scope."}, {"name": "unread", "in": "query", "schema": {"type": "string"}, "description": "Optional narrowing within current authorized scope."}, {"name": "team_id", "in": "query", "schema": {"type": "string"}, "description": "Optional narrowing within current authorized scope."}, {"name": "member_id", "in": "query", "schema": {"type": "string"}, "description": "Optional narrowing within current authorized scope."}, {"name": "phone_id", "in": "query", "schema": {"type": "string"}, "description": "Optional narrowing within current authorized scope."}]
      x-permission: "inbox.view"
      description: "Session-derived organization and current resource scope are checked on every call. Writes require verified email, matching Origin and session-bound CSRF. Registry, budget creation, note redaction and sending settings require recent reauthentication and confirmed MFA. Paid/template authority remains CLOSED."
      responses: {"200": {"description": "Scoped operation result; a queued or accepted intent is not delivery proof.", "content": {"application/json": {"schema": {"$ref": "#/components/schemas/ConversationPage"}}}}, "403": {"$ref": "#/components/responses/Error"}, "409": {"$ref": "#/components/responses/Error"}, "default": {"$ref": "#/components/responses/Error"}}
  /api/v1/conversations/{id}:
    get:
      operationId: "inboxGETconversationsid"
      summary: "Read scoped thread and presence"
      tags: ["Inbox"]
      security: [{"SessionCookie": []}]
      parameters: [{"$ref": "#/components/parameters/RequestID"}, {"name": "id", "in": "path", "required": true, "schema": {"type": "string", "format": "uuid"}}, {"name": "before", "in": "query", "schema": {"type": "string"}, "description": "Optional narrowing within current authorized scope."}]
      x-permission: "inbox.view"
      description: "Session-derived organization and current resource scope are checked on every call. Writes require verified email, matching Origin and session-bound CSRF. Registry, budget creation, note redaction and sending settings require recent reauthentication and confirmed MFA. Paid/template authority remains CLOSED."
      responses: {"200": {"description": "Scoped operation result; a queued or accepted intent is not delivery proof.", "content": {"application/json": {"schema": {"$ref": "#/components/schemas/InboxThread"}}}}, "403": {"$ref": "#/components/responses/Error"}, "409": {"$ref": "#/components/responses/Error"}, "default": {"$ref": "#/components/responses/Error"}}
    patch:
      operationId: "inboxPATCHconversationsid"
      summary: "Change status priority or human handoff"
      tags: ["Inbox"]
      security: [{"SessionCookie": []}]
      parameters: [{"$ref": "#/components/parameters/RequestID"}, {"name": "id", "in": "path", "required": true, "schema": {"type": "string", "format": "uuid"}}, {"$ref": "#/components/parameters/CSRF"}, {"$ref": "#/components/parameters/Origin"}]
      x-permission: "conversations.manage"
      description: "Session-derived organization and current resource scope are checked on every call. Writes require verified email, matching Origin and session-bound CSRF. Registry, budget creation, note redaction and sending settings require recent reauthentication and confirmed MFA. Paid/template authority remains CLOSED."
      requestBody: {"required": true, "content": {"application/json": {"schema": {"$ref": "#/components/schemas/ConversationPatch"}}}}
      responses: {"200": {"description": "Scoped operation result; a queued or accepted intent is not delivery proof.", "content": {"application/json": {"schema": {"$ref": "#/components/schemas/InboxCommand"}}}}, "403": {"$ref": "#/components/responses/Error"}, "409": {"$ref": "#/components/responses/Error"}, "default": {"$ref": "#/components/responses/Error"}}
  /api/v1/conversations/{id}/assignment:
    put:
      operationId: "inboxPUTconversationsidassignment"
      summary: "Replace assignment with revision check"
      tags: ["Inbox"]
      security: [{"SessionCookie": []}]
      parameters: [{"$ref": "#/components/parameters/RequestID"}, {"name": "id", "in": "path", "required": true, "schema": {"type": "string", "format": "uuid"}}, {"$ref": "#/components/parameters/CSRF"}, {"$ref": "#/components/parameters/Origin"}]
      x-permission: "conversations.assign"
      description: "Session-derived organization and current resource scope are checked on every call. Writes require verified email, matching Origin and session-bound CSRF. Registry, budget creation, note redaction and sending settings require recent reauthentication and confirmed MFA. Paid/template authority remains CLOSED."
      requestBody: {"required": true, "content": {"application/json": {"schema": {"$ref": "#/components/schemas/AssignmentInput"}}}}
      responses: {"200": {"description": "Scoped operation result; a queued or accepted intent is not delivery proof.", "content": {"application/json": {"schema": {"$ref": "#/components/schemas/InboxCommand"}}}}, "403": {"$ref": "#/components/responses/Error"}, "409": {"$ref": "#/components/responses/Error"}, "default": {"$ref": "#/components/responses/Error"}}
  /api/v1/conversations/{id}/read:
    post:
      operationId: "inboxPOSTconversationsidread"
      summary: "Advance own unread watermark"
      tags: ["Inbox"]
      security: [{"SessionCookie": []}]
      parameters: [{"$ref": "#/components/parameters/RequestID"}, {"name": "id", "in": "path", "required": true, "schema": {"type": "string", "format": "uuid"}}, {"$ref": "#/components/parameters/CSRF"}, {"$ref": "#/components/parameters/Origin"}]
      x-permission: "inbox.view"
      description: "Session-derived organization and current resource scope are checked on every call. Writes require verified email, matching Origin and session-bound CSRF. Registry, budget creation, note redaction and sending settings require recent reauthentication and confirmed MFA. Paid/template authority remains CLOSED."
      requestBody: {"required": true, "content": {"application/json": {"schema": {"$ref": "#/components/schemas/ReadInput"}}}}
      responses: {"200": {"description": "Scoped operation result; a queued or accepted intent is not delivery proof.", "content": {"application/json": {"schema": {"$ref": "#/components/schemas/InboxCommand"}}}}, "403": {"$ref": "#/components/responses/Error"}, "409": {"$ref": "#/components/responses/Error"}, "default": {"$ref": "#/components/responses/Error"}}
  /api/v1/conversations/{id}/notes:
    post:
      operationId: "inboxPOSTconversationsidnotes"
      summary: "Create internal note and scoped mentions"
      tags: ["Inbox"]
      security: [{"SessionCookie": []}]
      parameters: [{"$ref": "#/components/parameters/RequestID"}, {"name": "id", "in": "path", "required": true, "schema": {"type": "string", "format": "uuid"}}, {"$ref": "#/components/parameters/CSRF"}, {"$ref": "#/components/parameters/Origin"}]
      x-permission: "notes.write"
      description: "Session-derived organization and current resource scope are checked on every call. Writes require verified email, matching Origin and session-bound CSRF. Registry, budget creation, note redaction and sending settings require recent reauthentication and confirmed MFA. Paid/template authority remains CLOSED."
      requestBody: {"required": true, "content": {"application/json": {"schema": {"$ref": "#/components/schemas/NoteInput"}}}}
      responses: {"200": {"description": "Scoped operation result; a queued or accepted intent is not delivery proof.", "content": {"application/json": {"schema": {"$ref": "#/components/schemas/InboxCommand"}}}}, "403": {"$ref": "#/components/responses/Error"}, "409": {"$ref": "#/components/responses/Error"}, "default": {"$ref": "#/components/responses/Error"}}
  /api/v1/notes/{id}:
    patch:
      operationId: "inboxPATCHnotesid"
      summary: "Edit own internal note"
      tags: ["Inbox"]
      security: [{"SessionCookie": []}]
      parameters: [{"$ref": "#/components/parameters/RequestID"}, {"name": "id", "in": "path", "required": true, "schema": {"type": "string", "format": "uuid"}}, {"$ref": "#/components/parameters/CSRF"}, {"$ref": "#/components/parameters/Origin"}]
      x-permission: "notes.write"
      description: "Session-derived organization and current resource scope are checked on every call. Writes require verified email, matching Origin and session-bound CSRF. Registry, budget creation, note redaction and sending settings require recent reauthentication and confirmed MFA. Paid/template authority remains CLOSED."
      requestBody: {"required": true, "content": {"application/json": {"schema": {"$ref": "#/components/schemas/NoteEdit"}}}}
      responses: {"200": {"description": "Scoped operation result; a queued or accepted intent is not delivery proof.", "content": {"application/json": {"schema": {"$ref": "#/components/schemas/InboxCommand"}}}}, "403": {"$ref": "#/components/responses/Error"}, "409": {"$ref": "#/components/responses/Error"}, "default": {"$ref": "#/components/responses/Error"}}
  /api/v1/notes/{id}/redact:
    post:
      operationId: "inboxPOSTnotesidredact"
      summary: "Redact internal note with recent MFA"
      tags: ["Inbox"]
      security: [{"SessionCookie": []}]
      parameters: [{"$ref": "#/components/parameters/RequestID"}, {"name": "id", "in": "path", "required": true, "schema": {"type": "string", "format": "uuid"}}, {"$ref": "#/components/parameters/CSRF"}, {"$ref": "#/components/parameters/Origin"}]
      x-permission: "notes.redact"
      description: "Session-derived organization and current resource scope are checked on every call. Writes require verified email, matching Origin and session-bound CSRF. Registry, budget creation, note redaction and sending settings require recent reauthentication and confirmed MFA. Paid/template authority remains CLOSED."
      requestBody: {"required": true, "content": {"application/json": {"schema": {"$ref": "#/components/schemas/RevisionInput"}}}}
      responses: {"200": {"description": "Scoped operation result; a queued or accepted intent is not delivery proof.", "content": {"application/json": {"schema": {"$ref": "#/components/schemas/InboxCommand"}}}}, "403": {"$ref": "#/components/responses/Error"}, "409": {"$ref": "#/components/responses/Error"}, "default": {"$ref": "#/components/responses/Error"}}
  /api/v1/conversations/{id}/presence:
    post:
      operationId: "inboxPOSTconversationsidpresence"
      summary: "Renew advisory presence for 20 seconds"
      tags: ["Inbox"]
      security: [{"SessionCookie": []}]
      parameters: [{"$ref": "#/components/parameters/RequestID"}, {"name": "id", "in": "path", "required": true, "schema": {"type": "string", "format": "uuid"}}, {"$ref": "#/components/parameters/CSRF"}, {"$ref": "#/components/parameters/Origin"}]
      x-permission: "inbox.view"
      description: "Session-derived organization and current resource scope are checked on every call. Writes require verified email, matching Origin and session-bound CSRF. Registry, budget creation, note redaction and sending settings require recent reauthentication and confirmed MFA. Paid/template authority remains CLOSED."
      requestBody: {"required": true, "content": {"application/json": {"schema": {"$ref": "#/components/schemas/EmptyInput"}}}}
      responses: {"200": {"description": "Scoped operation result; a queued or accepted intent is not delivery proof.", "content": {"application/json": {"schema": {"$ref": "#/components/schemas/InboxCommand"}}}}, "403": {"$ref": "#/components/responses/Error"}, "409": {"$ref": "#/components/responses/Error"}, "default": {"$ref": "#/components/responses/Error"}}
  /api/v1/inbox/events:
    get:
      operationId: "inboxGETinboxevents"
      summary: "Stream scoped invalidations with bounded reconnect"
      tags: ["Inbox"]
      security: [{"SessionCookie": []}]
      parameters: [{"$ref": "#/components/parameters/RequestID"}]
      x-permission: "inbox.view"
      description: "Session-derived organization and current resource scope are checked on every call. Writes require verified email, matching Origin and session-bound CSRF. Registry, budget creation, note redaction and sending settings require recent reauthentication and confirmed MFA. Paid/template authority remains CLOSED. Invalidation events contain only scoped IDs and event types, never message text. REST is authoritative; clients refresh on connect and each event. Stream revalidates every four seconds and reconnects after 24 seconds; gaps trigger REST refresh, not cursor assumptions."
      responses: {"200": {"description": "Scoped operation result; a queued or accepted intent is not delivery proof.", "content": {"text/event-stream": {"schema": {"type": "string"}}}}, "403": {"$ref": "#/components/responses/Error"}, "409": {"$ref": "#/components/responses/Error"}, "default": {"$ref": "#/components/responses/Error"}}
  /api/v1/pricing/preflight:
    post:
      operationId: "inboxPOSTpricingpreflight"
      summary: "Evaluate finalizable text eligibility"
      tags: ["Inbox"]
      security: [{"SessionCookie": []}]
      parameters: [{"$ref": "#/components/parameters/RequestID"}, {"$ref": "#/components/parameters/CSRF"}, {"$ref": "#/components/parameters/Origin"}]
      x-permission: "messages.send"
      description: "Session-derived organization and current resource scope are checked on every call. Writes require verified email, matching Origin and session-bound CSRF. Registry, budget creation, note redaction and sending settings require recent reauthentication and confirmed MFA. Paid/template authority remains CLOSED."
      requestBody: {"required": true, "content": {"application/json": {"schema": {"$ref": "#/components/schemas/PreflightInput"}}}}
      responses: {"200": {"description": "Scoped operation result; a queued or accepted intent is not delivery proof.", "content": {"application/json": {"schema": {"$ref": "#/components/schemas/SendPreflight"}}}}, "403": {"$ref": "#/components/responses/Error"}, "409": {"$ref": "#/components/responses/Error"}, "default": {"$ref": "#/components/responses/Error"}}
  /api/v1/outbound-intents:
    post:
      operationId: "inboxPOSToutboundintents"
      summary: "Create immutable idempotent text intent"
      tags: ["Inbox"]
      security: [{"SessionCookie": []}]
      parameters: [{"$ref": "#/components/parameters/RequestID"}, {"$ref": "#/components/parameters/CSRF"}, {"$ref": "#/components/parameters/Origin"}]
      x-permission: "messages.send"
      description: "Session-derived organization and current resource scope are checked on every call. Writes require verified email, matching Origin and session-bound CSRF. Registry, budget creation, note redaction and sending settings require recent reauthentication and confirmed MFA. Paid/template authority remains CLOSED. The worker rechecks authorization before one network attempt. UNCERTAIN is never automatically resent. GET recovery returns only the current actor/session matching the client key."
      requestBody: {"required": true, "content": {"application/json": {"schema": {"$ref": "#/components/schemas/SubmitIntentInput"}}}}
      responses: {"200": {"description": "Scoped operation result; a queued or accepted intent is not delivery proof.", "content": {"application/json": {"schema": {"$ref": "#/components/schemas/InboxCommand"}}}}, "403": {"$ref": "#/components/responses/Error"}, "409": {"$ref": "#/components/responses/Error"}, "default": {"$ref": "#/components/responses/Error"}}
    get:
      operationId: "inboxGEToutboundintents"
      summary: "Recover own session intent by client key"
      tags: ["Inbox"]
      security: [{"SessionCookie": []}]
      parameters: [{"$ref": "#/components/parameters/RequestID"}, {"name": "client_key", "in": "query", "required": true, "schema": {"type": "string", "minLength": 16, "maxLength": 128}}]
      x-permission: "inbox.view"
      description: "Session-derived organization and current resource scope are checked on every call. Writes require verified email, matching Origin and session-bound CSRF. Registry, budget creation, note redaction and sending settings require recent reauthentication and confirmed MFA. Paid/template authority remains CLOSED. The worker rechecks authorization before one network attempt. UNCERTAIN is never automatically resent. GET recovery returns only the current actor/session matching the client key."
      responses: {"200": {"description": "Scoped operation result; a queued or accepted intent is not delivery proof.", "content": {"application/json": {"schema": {"type": "array", "items": {"$ref": "#/components/schemas/OutboundIntent"}}}}}, "403": {"$ref": "#/components/responses/Error"}, "409": {"$ref": "#/components/responses/Error"}, "default": {"$ref": "#/components/responses/Error"}}
  /api/v1/outbound-intents/{id}:
    get:
      operationId: "inboxGEToutboundintentsid"
      summary: "Read scoped intent outcome"
      tags: ["Inbox"]
      security: [{"SessionCookie": []}]
      parameters: [{"$ref": "#/components/parameters/RequestID"}, {"name": "id", "in": "path", "required": true, "schema": {"type": "string", "format": "uuid"}}]
      x-permission: "inbox.view"
      description: "Session-derived organization and current resource scope are checked on every call. Writes require verified email, matching Origin and session-bound CSRF. Registry, budget creation, note redaction and sending settings require recent reauthentication and confirmed MFA. Paid/template authority remains CLOSED."
      responses: {"200": {"description": "Scoped operation result; a queued or accepted intent is not delivery proof.", "content": {"application/json": {"schema": {"type": "array", "items": {"$ref": "#/components/schemas/OutboundIntent"}}}}}, "403": {"$ref": "#/components/responses/Error"}, "409": {"$ref": "#/components/responses/Error"}, "default": {"$ref": "#/components/responses/Error"}}
  /api/v1/pricing/billing-currency:
    put:
      operationId: "recordBillingCurrencyEvidence"
      summary: "Record reviewed company billing currency"
      tags: ["Inbox"]
      security: [{"SessionCookie": []}]
      parameters: [{"$ref": "#/components/parameters/RequestID"}, {"$ref": "#/components/parameters/CSRF"}, {"$ref": "#/components/parameters/Origin"}]
      x-permission: "pricing.registry.publish"
      description: "Verified email, matching Origin/CSRF, recent reauthentication and confirmed MFA required. Binds operator-reviewed currency evidence to this organization and WABA; invalidates old pricing evidence through policy revision. Does not publish rates, approve a budget or authorize a send."
      requestBody: {"required": true, "content": {"application/json": {"schema": {"$ref": "#/components/schemas/BillingCurrencyEvidenceInput"}}}}
      responses: {"200": {"description": "Scoped pricing result; no dispatch or budget authority.", "content": {"application/json": {"schema": {"$ref": "#/components/schemas/BillingCurrencyEvidenceResult"}}}}, "403": {"$ref": "#/components/responses/Error"}, "409": {"$ref": "#/components/responses/Error"}, "default": {"$ref": "#/components/responses/Error"}}
  /api/v1/pricing/service-exposure:
    post:
      operationId: "planControlledServiceExposure"
      summary: "Check published coverage for one controlled TEST Service reply"
      tags: ["Inbox"]
      security: [{"SessionCookie": []}]
      parameters: [{"$ref": "#/components/parameters/RequestID"}, {"$ref": "#/components/parameters/CSRF"}, {"$ref": "#/components/parameters/Origin"}]
      x-permission: "pricing.view"
      description: "Pricing-only planning for the configured TEST recipient and ID market, using server time plus 30 days and reviewed WABA timezone. Requires verified email, Origin/CSRF and session-derived organization. Reads immutable published intervals; complete coverage returns an INTERNAL_ESTIMATE and an unactivated one-message budget proposal, never a send token, intent or reservation. Quote lasts at most 60 seconds and no longer than review/coverage validity. Unknown allowances are ignored; incomplete coverage returns PRICING_HORIZON_NOT_FULLY_COVERED."
      requestBody: {"required": true, "content": {"application/json": {"schema": {"$ref": "#/components/schemas/ServiceExposureInput"}}}}
      responses: {"200": {"description": "Scoped pricing result; no dispatch or budget authority.", "content": {"application/json": {"schema": {"$ref": "#/components/schemas/ServiceExposurePreflight"}}}}, "403": {"$ref": "#/components/responses/Error"}, "409": {"$ref": "#/components/responses/Error"}, "default": {"$ref": "#/components/responses/Error"}}
  /api/v1/pricing:
    get:
      operationId: "inboxGETpricing"
      summary: "Inspect rate registry and independent zero-cost policy"
      tags: ["Inbox"]
      security: [{"SessionCookie": []}]
      parameters: [{"$ref": "#/components/parameters/RequestID"}]
      x-permission: "pricing.view"
      description: "Session-derived organization and current resource scope are checked on every call. Writes require verified email, matching Origin and session-bound CSRF. Registry, budget creation, note redaction and sending settings require recent reauthentication and confirmed MFA. Paid/template authority remains CLOSED."
      responses: {"200": {"description": "Scoped operation result; a queued or accepted intent is not delivery proof.", "content": {"application/json": {"schema": {"$ref": "#/components/schemas/PricingRegistry"}}}}, "403": {"$ref": "#/components/responses/Error"}, "409": {"$ref": "#/components/responses/Error"}, "default": {"$ref": "#/components/responses/Error"}}
  /api/v1/rate-cards/imports:
    post:
      operationId: "inboxPOSTratecardsimports"
      summary: "Import reviewed independent rate artifacts"
      tags: ["Inbox"]
      security: [{"SessionCookie": []}]
      parameters: [{"$ref": "#/components/parameters/RequestID"}, {"$ref": "#/components/parameters/CSRF"}, {"$ref": "#/components/parameters/Origin"}]
      x-permission: "pricing.registry.import"
      description: "Session-derived organization and current resource scope are checked on every call. Writes require verified email, matching Origin and session-bound CSRF. Registry, budget creation, note redaction and sending settings require recent reauthentication and confirmed MFA. Paid/template authority remains CLOSED."
      requestBody: {"required": true, "content": {"application/json": {"schema": {"$ref": "#/components/schemas/RateImportInput"}}}}
      responses: {"200": {"description": "Scoped operation result; a queued or accepted intent is not delivery proof.", "content": {"application/json": {"schema": {"$ref": "#/components/schemas/InboxCommand"}}}}, "403": {"$ref": "#/components/responses/Error"}, "409": {"$ref": "#/components/responses/Error"}, "default": {"$ref": "#/components/responses/Error"}}
  /api/v1/rate-cards/imports/{id}/{action}:
    post:
      operationId: "inboxPOSTratecardsimportsidaction"
      summary: "Advance reviewed registry lifecycle"
      tags: ["Inbox"]
      security: [{"SessionCookie": []}]
      parameters: [{"$ref": "#/components/parameters/RequestID"}, {"name": "id", "in": "path", "required": true, "schema": {"type": "string", "format": "uuid"}}, {"name": "action", "in": "path", "required": true, "schema": {"type": "string", "enum": ["validate", "diff", "submit", "review", "revise", "publish"]}}, {"$ref": "#/components/parameters/CSRF"}, {"$ref": "#/components/parameters/Origin"}]
      x-permission: "action-specific registry permission"
      description: "Session-derived organization and current resource scope are checked on every call. Writes require verified email, matching Origin and session-bound CSRF. Registry, budget creation, note redaction and sending settings require recent reauthentication and confirmed MFA. Paid/template authority remains CLOSED."
      requestBody: {"required": true, "content": {"application/json": {"schema": {"$ref": "#/components/schemas/RegistryActionInput"}}}}
      responses: {"200": {"description": "Scoped operation result; a queued or accepted intent is not delivery proof.", "content": {"application/json": {"schema": {"$ref": "#/components/schemas/InboxCommand"}}}}, "403": {"$ref": "#/components/responses/Error"}, "409": {"$ref": "#/components/responses/Error"}, "default": {"$ref": "#/components/responses/Error"}}
  /api/v1/budgets:
    get:
      operationId: "inboxGETbudgets"
      summary: "Read currency-qualified budget exposure"
      tags: ["Inbox"]
      security: [{"SessionCookie": []}]
      parameters: [{"$ref": "#/components/parameters/RequestID"}]
      x-permission: "pricing.view"
      description: "Session-derived organization and current resource scope are checked on every call. Writes require verified email, matching Origin and session-bound CSRF. Registry, budget creation, note redaction and sending settings require recent reauthentication and confirmed MFA. Paid/template authority remains CLOSED."
      responses: {"200": {"description": "Scoped operation result; a queued or accepted intent is not delivery proof.", "content": {"application/json": {"schema": {"type": "array", "items": {"$ref": "#/components/schemas/BudgetExposure"}}}}}, "403": {"$ref": "#/components/responses/Error"}, "409": {"$ref": "#/components/responses/Error"}, "default": {"$ref": "#/components/responses/Error"}}
    post:
      operationId: "inboxPOSTbudgets"
      summary: "Create budget with verified account currency"
      tags: ["Inbox"]
      security: [{"SessionCookie": []}]
      parameters: [{"$ref": "#/components/parameters/RequestID"}, {"$ref": "#/components/parameters/CSRF"}, {"$ref": "#/components/parameters/Origin"}]
      x-permission: "budgets.manage"
      description: "Session-derived organization and current resource scope are checked on every call. Writes require verified email, matching Origin and session-bound CSRF. Registry, budget creation, note redaction and sending settings require recent reauthentication and confirmed MFA. Paid/template authority remains CLOSED."
      requestBody: {"required": true, "content": {"application/json": {"schema": {"$ref": "#/components/schemas/BudgetInput"}}}}
      responses: {"200": {"description": "Scoped operation result; a queued or accepted intent is not delivery proof.", "content": {"application/json": {"schema": {"$ref": "#/components/schemas/InboxCommand"}}}}, "403": {"$ref": "#/components/responses/Error"}, "409": {"$ref": "#/components/responses/Error"}, "default": {"$ref": "#/components/responses/Error"}}
  /api/v1/inbox/settings:
    patch:
      operationId: "inboxPATCHinboxsettings"
      summary: "Change sending switch and policy revision"
      tags: ["Inbox"]
      security: [{"SessionCookie": []}]
      parameters: [{"$ref": "#/components/parameters/RequestID"}, {"$ref": "#/components/parameters/CSRF"}, {"$ref": "#/components/parameters/Origin"}]
      x-permission: "sending.manage"
      description: "Session-derived organization and current resource scope are checked on every call. Writes require verified email, matching Origin and session-bound CSRF. Registry, budget creation, note redaction and sending settings require recent reauthentication and confirmed MFA. Paid/template authority remains CLOSED."
      requestBody: {"required": true, "content": {"application/json": {"schema": {"$ref": "#/components/schemas/SendingSettingsInput"}}}}
      responses: {"200": {"description": "Scoped operation result; a queued or accepted intent is not delivery proof.", "content": {"application/json": {"schema": {"$ref": "#/components/schemas/InboxCommand"}}}}, "403": {"$ref": "#/components/responses/Error"}, "409": {"$ref": "#/components/responses/Error"}, "default": {"$ref": "#/components/responses/Error"}}
  /api/v1/meta/connection:
    get:
      operationId: metaGETmetaconnection
      summary: List configured connections
      tags: [Meta]
      security:
        - SessionCookie: []
      x-permission: meta.view
      description: Organization is derived from the authenticated session. Mutations require verified email, recent authentication, production MFA, Origin and session-bound CSRF. No outbound Meta operation is performed.
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - name: cursor
          in: query
          schema:
            type: string
            format: uuid
          description: Accessible row cursor; up to 100 rows per response.
      responses:
        '200':
          description: Successful operation or durably queued work.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MetaConnection'
        '403':
          $ref: '#/components/responses/Error'
        default:
          $ref: '#/components/responses/Error'
    post:
      operationId: metaPOSTmetaconnection
      summary: Bind operator-configured App to the session organization
      tags: [Meta]
      security:
        - SessionCookie: []
      x-permission: meta.manage
      description: Organization is derived from the authenticated session. Mutations require verified email, recent authentication, production MFA, Origin and session-bound CSRF. No outbound Meta operation is performed.
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
      responses:
        '200':
          description: Successful operation or durably queued work.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MetaCommand'
        '403':
          $ref: '#/components/responses/Error'
        default:
          $ref: '#/components/responses/Error'
  /api/v1/meta/sync:
    post:
      operationId: metaPOSTmetasync
      summary: Queue a read-only asset synchronization
      tags: [Meta]
      security:
        - SessionCookie: []
      x-permission: meta.manage
      description: Organization is derived from the authenticated session. Mutations require verified email, recent authentication, production MFA, Origin and session-bound CSRF. No outbound Meta operation is performed.
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
      responses:
        '200':
          description: Successful operation or durably queued work.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MetaCommand'
        '403':
          $ref: '#/components/responses/Error'
        default:
          $ref: '#/components/responses/Error'
  /api/v1/meta/sync-runs:
    get:
      operationId: metaGETmetasyncruns
      summary: List synchronization evidence
      tags: [Meta]
      security:
        - SessionCookie: []
      x-permission: meta.view
      description: Organization is derived from the authenticated session. Mutations require verified email, recent authentication, production MFA, Origin and session-bound CSRF. No outbound Meta operation is performed.
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - name: cursor
          in: query
          schema:
            type: string
            format: uuid
          description: Accessible row cursor; up to 100 rows per response.
      responses:
        '200':
          description: Successful operation or durably queued work.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MetaSync'
        '403':
          $ref: '#/components/responses/Error'
        default:
          $ref: '#/components/responses/Error'
  /api/v1/meta/wabas:
    get:
      operationId: metaGETmetawabas
      summary: List synchronized WABAs
      tags: [Meta]
      security:
        - SessionCookie: []
      x-permission: meta.view
      description: Organization is derived from the authenticated session. Mutations require verified email, recent authentication, production MFA, Origin and session-bound CSRF. No outbound Meta operation is performed.
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - name: cursor
          in: query
          schema:
            type: string
            format: uuid
          description: Accessible row cursor; up to 100 rows per response.
      responses:
        '200':
          description: Successful operation or durably queued work.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MetaWABA'
        '403':
          $ref: '#/components/responses/Error'
        default:
          $ref: '#/components/responses/Error'
  /api/v1/meta/phone-numbers:
    get:
      operationId: metaGETmetaphonenumbers
      summary: List synchronized phone numbers
      tags: [Meta]
      security:
        - SessionCookie: []
      x-permission: meta.view
      description: Organization is derived from the authenticated session. Mutations require verified email, recent authentication, production MFA, Origin and session-bound CSRF. No outbound Meta operation is performed.
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - name: cursor
          in: query
          schema:
            type: string
            format: uuid
          description: Accessible row cursor; up to 100 rows per response.
      responses:
        '200':
          description: Successful operation or durably queued work.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MetaPhone'
        '403':
          $ref: '#/components/responses/Error'
        default:
          $ref: '#/components/responses/Error'
  /api/v1/meta/business-profiles:
    get:
      operationId: metaGETmetabusinessprofiles
      summary: List synchronized business profiles
      tags: [Meta]
      security:
        - SessionCookie: []
      x-permission: meta.view
      description: Organization is derived from the authenticated session. Mutations require verified email, recent authentication, production MFA, Origin and session-bound CSRF. No outbound Meta operation is performed.
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - name: cursor
          in: query
          schema:
            type: string
            format: uuid
          description: Accessible row cursor; up to 100 rows per response.
      responses:
        '200':
          description: Successful operation or durably queued work.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MetaProfile'
        '403':
          $ref: '#/components/responses/Error'
        default:
          $ref: '#/components/responses/Error'
  /api/v1/meta/health:
    get:
      operationId: metaGETmetahealth
      summary: Read Meta observations and local processing counts
      tags: [Meta]
      security:
        - SessionCookie: []
      x-permission: meta.view
      description: Organization is derived from the authenticated session. Mutations require verified email, recent authentication, production MFA, Origin and session-bound CSRF. No outbound Meta operation is performed.
      parameters:
        - $ref: '#/components/parameters/RequestID'
      responses:
        '200':
          description: Successful operation or durably queued work.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MetaHealth'
        '403':
          $ref: '#/components/responses/Error'
        default:
          $ref: '#/components/responses/Error'
  /api/v1/meta/webhook-events:
    get:
      operationId: metaGETmetawebhookevents
      summary: List sanitized event diagnostics
      tags: [Meta]
      security:
        - SessionCookie: []
      x-permission: webhooks.view
      description: Organization is derived from the authenticated session. Mutations require verified email, recent authentication, production MFA, Origin and session-bound CSRF. No outbound Meta operation is performed.
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - name: cursor
          in: query
          schema:
            type: string
            format: uuid
          description: Accessible row cursor; up to 100 rows per response.
      responses:
        '200':
          description: Successful operation or durably queued work.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MetaEvent'
        '403':
          $ref: '#/components/responses/Error'
        default:
          $ref: '#/components/responses/Error'
  /api/v1/meta/webhook-events/{id}:
    get:
      operationId: metaGETmetawebhookeventsid
      summary: Inspect event attempts and classified facts
      tags: [Meta]
      security:
        - SessionCookie: []
      x-permission: webhooks.view
      description: Organization is derived from the authenticated session. Mutations require verified email, recent authentication, production MFA, Origin and session-bound CSRF. No outbound Meta operation is performed.
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Successful operation or durably queued work.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MetaDetail'
        '403':
          $ref: '#/components/responses/Error'
        default:
          $ref: '#/components/responses/Error'
  /api/v1/meta/webhook-events/{id}/payload:
    get:
      operationId: metaGETmetawebhookeventsidpayload
      summary: Inspect audited redacted envelope structure
      tags: [Meta]
      security:
        - SessionCookie: []
      x-permission: webhooks.payload.view
      description: Organization is derived from the authenticated session. Mutations require verified email, recent authentication, production MFA, Origin and session-bound CSRF. No outbound Meta operation is performed.
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Successful operation or durably queued work.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MetaPayload'
        '403':
          $ref: '#/components/responses/Error'
        default:
          $ref: '#/components/responses/Error'
  /api/v1/meta/webhook-events/{id}/replay:
    post:
      operationId: metaPOSTmetawebhookeventsidreplay
      summary: Queue the immutable stored event for replay
      tags: [Meta]
      security:
        - SessionCookie: []
      x-permission: webhooks.replay
      description: Organization is derived from the authenticated session. Mutations require verified email, recent authentication, production MFA, Origin and session-bound CSRF. No outbound Meta operation is performed.
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
      responses:
        '200':
          description: Successful operation or durably queued work.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MetaCommand'
        '403':
          $ref: '#/components/responses/Error'
        default:
          $ref: '#/components/responses/Error'
  /api/v1/meta/webhooks/{callback}:
    get:
      operationId: metaWebhookget
      summary: Verify the configured callback
      tags: [Meta]
      security: []
      description: Only subscribe mode, one exact token and one nonempty challenge are accepted. Duplicate or malformed parameters fail closed.
      parameters:
        - name: callback
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: hub.mode
          in: query
          required: true
          schema:
            type: string
        - name: hub.verify_token
          in: query
          required: true
          schema:
            type: string
        - name: hub.challenge
          in: query
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Exact challenge string.
          content:
            text/plain:
              schema:
                type: string
        '403':
          description: Verification or signature rejected.
          content:
            text/plain:
              schema:
                type: string
        default:
          description: Rejected verification, signature, envelope, route, body limit or persistence. No success is returned for failed persistence.
          content:
            text/plain:
              schema:
                type: string
    post:
      operationId: metaWebhookpost
      summary: Authenticate and durably ingest an exact-byte envelope
      tags: [Meta]
      security: []
      description: App Secret HMAC-SHA256 is verified before JSON parsing and persistence. Raw encrypted evidence and transport dedupe commit before 200. Unknown asset bindings are quarantined. No unsigned mode. Application body cap is the lower of configured HTTP limit and 4 MiB.
      parameters:
        - name: callback
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: X-Hub-Signature-256
          in: header
          required: true
          schema:
            type: string
            pattern: '^sha256=[0-9a-fA-F]{64}$'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [object, entry]
              properties:
                object:
                  const: whatsapp_business_account
                entry:
                  type: array
                  minItems: 1
                  maxItems: 1000
                  items:
                    type: object
      responses:
        '200':
          description: Durable persistence confirmed; transport retries are safe.
          content:
            text/plain:
              schema:
                type: string
        '403':
          description: Verification or signature rejected.
          content:
            text/plain:
              schema:
                type: string
        default:
          description: Rejected verification, signature, envelope, route, body limit or persistence. No success is returned for failed persistence.
          content:
            text/plain:
              schema:
                type: string
  /healthz:
    get:
      operationId: getLiveness
      summary: Check process liveness
      description: Does not contact Meta, SMTP or PostgreSQL.
      tags:
        - Foundation
      parameters:
        - $ref: '#/components/parameters/RequestID'
      responses:
        '200':
          $ref: '#/components/responses/Health'
        '413':
          $ref: '#/components/responses/Error'
        default:
          $ref: '#/components/responses/Error'
  /readyz:
    get:
      operationId: getReadiness
      summary: Check database and schema readiness
      description: Requires compatible schema and a non-owner, non-bypass runtime role.
      tags:
        - Foundation
      parameters:
        - $ref: '#/components/parameters/RequestID'
      responses:
        '200':
          $ref: '#/components/responses/Health'
        '413':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
        default:
          $ref: '#/components/responses/Error'
  /api/v1/auth/csrf:
    get:
      operationId: csrf
      summary: csrf
      tags:
        - Identity
      security: []
      description: Return/initiate the same-origin CSRF cookie and readable token.
      x-permission: Public proof/throttling or CSRF bootstrap
      parameters:
        - $ref: '#/components/parameters/RequestID'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
  /api/v1/auth/login:
    post:
      operationId: public_login
      summary: public login
      tags:
        - Identity
      security: []
      description: >-
        Generic authentication failure. Successful password authentication returns a rotated session or a bounded MFA
        challenge.
      x-permission: Public proof/throttling or CSRF bootstrap
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required:
                - email
                - password
  /api/v1/auth/logout:
    post:
      operationId: logout
      summary: logout
      tags:
        - Identity
      security:
        - SessionCookie: []
      description: Revoke current session and expire its cookie.
      x-permission: Authenticated self
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required: []
  /api/v1/auth/session:
    get:
      operationId: session
      summary: session
      tags:
        - Identity
      security:
        - SessionCookie: []
      description: Return current nonsecret identity metadata and active organization memberships.
      x-permission: Authenticated self
      parameters:
        - $ref: '#/components/parameters/RequestID'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
  /api/v1/auth/mfa/verify:
    post:
      operationId: public_mfa
      summary: public mfa
      tags:
        - Identity
      security: []
      description: Consume an MFA login challenge after one-use TOTP/recovery-code validation; establish a rotated session.
      x-permission: Public proof/throttling or CSRF bootstrap
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required:
                - token
                - code
  /api/v1/auth/reauthenticate:
    post:
      operationId: reauth
      summary: reauth
      tags:
        - Identity
      security:
        - SessionCookie: []
      description: Password plus current factor if enrolled. Rotate session; five-minute reauthentication freshness.
      x-permission: Authenticated self
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required:
                - password
  /api/v1/auth/password-reset/request:
    post:
      operationId: public_reset_request
      summary: public reset request
      tags:
        - Identity
      security: []
      description: >-
        Generic 202 for known/unknown addresses. Atomically supersede the previous reset challenge and enqueue encrypted
        identity mail for eligible identities.
      x-permission: Public proof/throttling or CSRF bootstrap
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
      responses:
        '202':
          description: Accepted for asynchronous email delivery; not proof of inbox receipt.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required:
                - email
  /api/v1/auth/password-reset/complete:
    post:
      operationId: public_reset_complete
      summary: public reset complete
      tags:
        - Identity
      security: []
      description: >-
        Consume a single-use 30-minute reset challenge, change the password, invalidate old sessions and enqueue a
        security notice.
      x-permission: Public proof/throttling or CSRF bootstrap
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required:
                - token
                - password
  /api/v1/auth/email-verification/request:
    post:
      operationId: public_verify_request
      summary: public verify request
      tags:
        - Identity
      security: []
      description: Generic 202. Enqueue verification for eligible unverified identities.
      x-permission: Public proof/throttling or CSRF bootstrap
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
      responses:
        '202':
          description: Accepted for asynchronous email delivery; not proof of inbox receipt.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required:
                - email
  /api/v1/auth/email-verification/complete:
    post:
      operationId: public_verify_complete
      summary: public verify complete
      tags:
        - Identity
      security: []
      description: Consume a single-use 24-hour verification challenge; GET never consumes proof.
      x-permission: Public proof/throttling or CSRF bootstrap
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required:
                - token
  /api/v1/auth/organization-session:
    post:
      operationId: organization_select
      summary: organization select
      tags:
        - Identity
      security:
        - SessionCookie: []
      description: Validate current membership and rotate to an organization-bound session.
      x-permission: Authenticated self
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required:
                - organization_id
  /api/v1/auth/mfa/enrollment:
    post:
      operationId: mfa_enroll
      summary: mfa enroll
      tags:
        - Identity
      security:
        - SessionCookie: []
      description: Recent-authentication required. Return the setup secret only during enrollment; store envelope-encrypted secret.
      x-permission: Authenticated self
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required: []
  /api/v1/auth/mfa/enrollment/confirm:
    post:
      operationId: mfa_confirm
      summary: mfa confirm
      tags:
        - Identity
      security:
        - SessionCookie: []
      description: Confirm authenticator possession, enable TOTP, invalidate prior sessions and show ten recovery codes once.
      x-permission: Authenticated self
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required:
                - code
  /api/v1/auth/mfa/disable:
    post:
      operationId: mfa_disable
      summary: mfa disable
      tags:
        - Identity
      security:
        - SessionCookie: []
      description: Recent-authentication required. Remove factor/codes, invalidate prior sessions and send a security notice.
      x-permission: Authenticated self
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required: []
  /api/v1/auth/mfa/recovery-codes/regenerate:
    post:
      operationId: mfa_recovery
      summary: mfa recovery
      tags:
        - Identity
      security:
        - SessionCookie: []
      description: >-
        Recent-authentication required. Replace all recovery codes, invalidate prior sessions and send a security
        notice.
      x-permission: Authenticated self
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required: []
  /api/v1/security/sessions:
    get:
      operationId: sessions
      summary: sessions
      tags:
        - Identity
      security:
        - SessionCookie: []
      description: List own unrevoked/unexpired sessions; no bearer or CSRF digest.
      x-permission: Authenticated self
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - in: query
          name: cursor
          required: false
          description: >-
            Opaque signed continuation bound to the authenticated user, organization and endpoint. Reauthorization
            occurs on every page.
          schema:
            type: string
            maxLength: 128
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityList'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
  /api/v1/security/sessions/{id}/revoke:
    post:
      operationId: session_revoke
      summary: session revoke
      tags:
        - Identity
      security:
        - SessionCookie: []
      description: Revoke the path session only if owned by the caller; recent authentication required.
      x-permission: Authenticated self
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required: []
  /api/v1/security/sessions/revoke-others:
    post:
      operationId: sessions_revoke
      summary: sessions revoke
      tags:
        - Identity
      security:
        - SessionCookie: []
      description: Revoke all own sessions except the current one; recent authentication required.
      x-permission: Authenticated self
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required: []
  /api/v1/invitations/accept:
    post:
      operationId: public_invitation_accept
      summary: public invitation accept
      tags:
        - Identity
      security: []
      description: >-
        Consume invitation. New identities require name/password; existing identities must already authenticate as the
        bound email and must not submit a replacement password. Recheck inviter permissions and role ceiling.
      x-permission: Public proof/throttling or CSRF bootstrap
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required:
                - token
  /api/v1/organization:
    get:
      operationId: org_get
      summary: org get
      tags:
        - Organization
      security:
        - SessionCookie: []
      description: >-
        Return selected organization's metadata and current organization-wide permission keys. Authorization:
        organization.view. Mutations require verified email, recent authentication and enrolled MFA in production.
      x-permission: organization.view
      parameters:
        - $ref: '#/components/parameters/RequestID'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
  /api/v1/organizations/{org}/members:
    get:
      operationId: org_members_list
      summary: org members list
      tags:
        - Organization
      security:
        - SessionCookie: []
      description: >-
        List scoped resource summaries, bounded to 100 rows. Authorization: members.view. Mutations require verified
        email, recent authentication and enrolled MFA in production.
      x-permission: members.view
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - in: path
          name: org
          required: true
          schema:
            type: string
            format: uuid
        - in: query
          name: cursor
          required: false
          description: >-
            Opaque signed continuation bound to the authenticated user, organization and endpoint. Reauthorization
            occurs on every page.
          schema:
            type: string
            maxLength: 128
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityList'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
  /api/v1/organizations/{org}/members/{id}:
    get:
      operationId: org_member_get
      summary: org member get
      tags:
        - Organization
      security:
        - SessionCookie: []
      description: >-
        Apply the reviewed resource command atomically with audit. Authorization: members.view. Mutations require
        verified email, recent authentication and enrolled MFA in production.
      x-permission: members.view
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - in: path
          name: org
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityList'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
  /api/v1/organizations/{org}/members/{id}/deactivate:
    post:
      operationId: org_member_deactivate
      summary: org member deactivate
      tags:
        - Organization
      security:
        - SessionCookie: []
      description: >-
        Immediately deny future organization authorization, increment access revision and remove team memberships while
        preserving actor history. Authorization: members.manage + delegation ceiling + last-Owner invariant. Mutations
        require verified email, recent authentication and enrolled MFA in production.
      x-permission: members.manage + delegation ceiling + last-Owner invariant
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - in: path
          name: org
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
        - $ref: '#/components/parameters/Revision'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required: []
  /api/v1/organizations/{org}/members/{id}/reactivate:
    post:
      operationId: org_member_reactivate
      summary: org member reactivate
      tags:
        - Organization
      security:
        - SessionCookie: []
      description: >-
        Restore membership with retained role grants after delegation checks; removed team memberships are not restored.
        Authorization: members.manage + delegation ceiling. Mutations require verified email, recent authentication and
        enrolled MFA in production.
      x-permission: members.manage + delegation ceiling
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - in: path
          name: org
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
        - $ref: '#/components/parameters/Revision'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required: []
  /api/v1/organizations/{org}/members/{id}/role-grants:
    put:
      operationId: org_member_roles
      summary: org member roles
      tags:
        - Organization
      security:
        - SessionCookie: []
      description: >-
        Replace explicit ORG/TEAM/SELF role grants with cross-organization foreign-key enforcement. Authorization:
        roles.assign + old/new permission ceiling + last-Owner invariant. Mutations require verified email, recent
        authentication and enrolled MFA in production. Supports mixed grants[] or legacy role_ids with one scope.
        Replaces the complete direct grant set; team inheritance remains separate.
      x-permission: roles.assign + old/new permission ceiling + last-Owner invariant
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - in: path
          name: org
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
        - $ref: '#/components/parameters/Revision'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
                grants:
                  type: array
                  maxItems: 20
                  items:
                    type: object
                    additionalProperties: false
                    required:
                      - role_id
                      - scope
                      - team_id
                    properties:
                      role_id:
                        type: string
                        format: uuid
                      scope:
                        type: string
                        enum:
                          - ORG
                          - TEAM
                          - SELF
                      team_id:
                        type:
                          - string
                          - 'null'
                        format: uuid
              anyOf:
                - required:
                    - grants
                - required:
                    - role_ids
  /api/v1/organizations/{org}/invitations:
    get:
      operationId: org_invitations_list
      summary: org invitations list
      tags:
        - Organization
      security:
        - SessionCookie: []
      description: >-
        List scoped resource summaries, bounded to 100 rows. Authorization: members.invite. Mutations require verified
        email, recent authentication and enrolled MFA in production.
      x-permission: members.invite
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - in: path
          name: org
          required: true
          schema:
            type: string
            format: uuid
        - in: query
          name: cursor
          required: false
          description: >-
            Opaque signed continuation bound to the authenticated user, organization and endpoint. Reauthorization
            occurs on every page.
          schema:
            type: string
            maxLength: 128
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityList'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
    post:
      operationId: org_invite
      summary: org invite
      tags:
        - Organization
      security:
        - SessionCookie: []
      description: >-
        Create a 72-hour invitation and encrypted durable email. No account/password activation until explicit
        acceptance. Authorization: members.invite + delegation ceiling. Mutations require verified email, recent
        authentication and enrolled MFA in production.
      x-permission: members.invite + delegation ceiling
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - in: path
          name: org
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
      responses:
        '202':
          description: Accepted for asynchronous email delivery; not proof of inbox receipt.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required:
                - email
                - role_ids
  /api/v1/organizations/{org}/invitations/{id}/revoke:
    post:
      operationId: org_invite_revoke
      summary: org invite revoke
      tags:
        - Organization
      security:
        - SessionCookie: []
      description: >-
        Invalidate pending invitation and cancel unsent deliveries. Authorization: members.invite. Mutations require
        verified email, recent authentication and enrolled MFA in production.
      x-permission: members.invite
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - in: path
          name: org
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
        - $ref: '#/components/parameters/Revision'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required: []
  /api/v1/organizations/{org}/invitations/{id}/resend:
    post:
      operationId: org_invite_resend
      summary: org invite resend
      tags:
        - Organization
      security:
        - SessionCookie: []
      description: >-
        Supersede the invitation token/deadline, cancel unsent previous deliveries and enqueue the new generation.
        Authorization: members.invite + delegation ceiling. Mutations require verified email, recent authentication and
        enrolled MFA in production.
      x-permission: members.invite + delegation ceiling
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - in: path
          name: org
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
        - $ref: '#/components/parameters/Revision'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required: []
  /api/v1/organizations/{org}/teams:
    get:
      operationId: org_teams_list
      summary: org teams list
      tags:
        - Organization
      security:
        - SessionCookie: []
      description: >-
        List scoped resource summaries, bounded to 100 rows. Authorization: teams.view. Mutations require verified
        email, recent authentication and enrolled MFA in production.
      x-permission: teams.view
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - in: path
          name: org
          required: true
          schema:
            type: string
            format: uuid
        - in: query
          name: cursor
          required: false
          description: >-
            Opaque signed continuation bound to the authenticated user, organization and endpoint. Reauthorization
            occurs on every page.
          schema:
            type: string
            maxLength: 128
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityList'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
    post:
      operationId: org_team_create
      summary: org team create
      tags:
        - Organization
      security:
        - SessionCookie: []
      description: >-
        Apply the reviewed resource command atomically with audit. Authorization: teams.manage. Mutations require
        verified email, recent authentication and enrolled MFA in production.
      x-permission: teams.manage
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - in: path
          name: org
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
      responses:
        '201':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required:
                - name
  /api/v1/organizations/{org}/teams/{id}:
    patch:
      operationId: org_team_update
      summary: org team update
      tags:
        - Organization
      security:
        - SessionCookie: []
      description: >-
        Apply the reviewed resource command atomically with audit. Authorization: teams.manage within team scope.
        Mutations require verified email, recent authentication and enrolled MFA in production.
      x-permission: teams.manage within team scope
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - in: path
          name: org
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
        - $ref: '#/components/parameters/Revision'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required:
                - name
  /api/v1/organizations/{org}/teams/{id}/archive:
    post:
      operationId: org_team_archive
      summary: org team archive
      tags:
        - Organization
      security:
        - SessionCookie: []
      description: >-
        Apply the reviewed resource command atomically with audit. Authorization: teams.manage + current team-role
        delegation ceiling. Mutations require verified email, recent authentication and enrolled MFA in production.
      x-permission: teams.manage + current team-role delegation ceiling
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - in: path
          name: org
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
        - $ref: '#/components/parameters/Revision'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required: []
  /api/v1/organizations/{org}/teams/{id}/members:
    get:
      operationId: org_team_members_list
      summary: org team members list
      tags:
        - Organization
      security:
        - SessionCookie: []
      description: >-
        List scoped resource summaries, bounded to 100 rows. Authorization: teams.view within team scope. Mutations
        require verified email, recent authentication and enrolled MFA in production.
      x-permission: teams.view within team scope
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - in: path
          name: org
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
        - in: query
          name: cursor
          required: false
          description: >-
            Opaque signed continuation bound to the authenticated user, organization and endpoint. Reauthorization
            occurs on every page.
          schema:
            type: string
            maxLength: 128
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityList'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
    post:
      operationId: org_team_member_add
      summary: org team member add
      tags:
        - Organization
      security:
        - SessionCookie: []
      description: >-
        Apply the reviewed resource command atomically with audit. Authorization: teams.manage + current team-role
        delegation ceiling. Mutations require verified email, recent authentication and enrolled MFA in production.
      x-permission: teams.manage + current team-role delegation ceiling
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - in: path
          name: org
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
        - $ref: '#/components/parameters/Revision'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required:
                - member_id
  /api/v1/organizations/{org}/teams/{id}/members/{member}:
    delete:
      operationId: org_team_member_remove
      summary: org team member remove
      tags:
        - Organization
      security:
        - SessionCookie: []
      description: >-
        Apply the reviewed resource command atomically with audit. Authorization: teams.manage + current team-role
        delegation ceiling. Mutations require verified email, recent authentication and enrolled MFA in production.
      x-permission: teams.manage + current team-role delegation ceiling
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - in: path
          name: org
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: member
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
        - $ref: '#/components/parameters/Revision'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required: []
  /api/v1/organizations/{org}/teams/{id}/role-grants:
    put:
      operationId: org_team_roles
      summary: org team roles
      tags:
        - Organization
      security:
        - SessionCookie: []
      description: >-
        Replace team-role grants; inherited permissions remain scoped to the team. Authorization: roles.assign + team
        permission ceiling. Mutations require verified email, recent authentication and enrolled MFA in production.
      x-permission: roles.assign + team permission ceiling
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - in: path
          name: org
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
        - $ref: '#/components/parameters/Revision'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required:
                - role_ids
  /api/v1/organizations/{org}/roles:
    get:
      operationId: org_roles_list
      summary: org roles list
      tags:
        - Organization
      security:
        - SessionCookie: []
      description: >-
        List scoped resource summaries, bounded to 100 rows. Authorization: roles.view. Mutations require verified
        email, recent authentication and enrolled MFA in production.
      x-permission: roles.view
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - in: path
          name: org
          required: true
          schema:
            type: string
            format: uuid
        - in: query
          name: cursor
          required: false
          description: >-
            Opaque signed continuation bound to the authenticated user, organization and endpoint. Reauthorization
            occurs on every page.
          schema:
            type: string
            maxLength: 128
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityList'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
    post:
      operationId: org_role_create
      summary: org role create
      tags:
        - Organization
      security:
        - SessionCookie: []
      description: >-
        Apply the reviewed resource command atomically with audit. Authorization: roles.manage + delegation ceiling.
        Mutations require verified email, recent authentication and enrolled MFA in production.
      x-permission: roles.manage + delegation ceiling
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - in: path
          name: org
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
      responses:
        '201':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required:
                - name
                - permissions
  /api/v1/organizations/{org}/roles/{id}:
    patch:
      operationId: org_role_update
      summary: org role update
      tags:
        - Organization
      security:
        - SessionCookie: []
      description: >-
        Apply the reviewed resource command atomically with audit. Authorization: roles.manage + old/new permission
        ceiling + last-Owner invariant. Mutations require verified email, recent authentication and enrolled MFA in
        production.
      x-permission: roles.manage + old/new permission ceiling + last-Owner invariant
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - in: path
          name: org
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
        - $ref: '#/components/parameters/CSRF'
        - $ref: '#/components/parameters/Origin'
        - $ref: '#/components/parameters/Revision'
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityResult'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
                  minLength: 12
                  maxLength: 256
                  writeOnly: true
                code:
                  type: string
                  writeOnly: true
                token:
                  type: string
                  writeOnly: true
                name:
                  type: string
                  maxLength: 100
                organization_id:
                  type: string
                  format: uuid
                member_id:
                  type: string
                  format: uuid
                role_ids:
                  type: array
                  maxItems: 20
                  items:
                    type: string
                    format: uuid
                permissions:
                  type: array
                  maxItems: 30
                  items:
                    type: string
                    enum:
                      - organization.view
                      - members.view
                      - members.invite
                      - members.manage
                      - teams.view
                      - teams.manage
                      - roles.view
                      - roles.manage
                      - roles.assign
                      - owners.manage
                      - audit.view
                scope:
                  type: string
                  enum:
                    - ORG
                    - TEAM
                    - SELF
                team_id:
                  anyOf:
                    - type: string
                      format: uuid
                    - type: 'null'
              required:
                - name
                - permissions
  /api/v1/organizations/{org}/audit:
    get:
      operationId: org_audit_list
      summary: org audit list
      tags:
        - Organization
      security:
        - SessionCookie: []
      description: >-
        Read append-only redacted organization security events. Authorization: audit.view. Mutations require verified
        email, recent authentication and enrolled MFA in production.
      x-permission: audit.view
      parameters:
        - $ref: '#/components/parameters/RequestID'
        - in: path
          name: org
          required: true
          schema:
            type: string
            format: uuid
        - in: query
          name: cursor
          required: false
          description: >-
            Opaque signed continuation bound to the authenticated user, organization and endpoint. Reauthorization
            occurs on every page.
          schema:
            type: string
            maxLength: 128
      responses:
        '200':
          description: Successful response.
          headers:
            X-Request-ID:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IdentityList'
        '400':
          $ref: '#/components/responses/Error'
        '401':
          $ref: '#/components/responses/Error'
        '403':
          $ref: '#/components/responses/Error'
        '404':
          $ref: '#/components/responses/Error'
        '409':
          $ref: '#/components/responses/Error'
        '413':
          $ref: '#/components/responses/Error'
        '422':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
        '429':
          $ref: '#/components/responses/Error'
        '503':
          $ref: '#/components/responses/Error'
components:
  securitySchemes:
    SessionCookie:
      type: apiKey
      in: cookie
      name: waba_session
      description: >-
        Opaque server-side session. HttpOnly; Secure in production; SameSite=Lax; 12-hour absolute and 30-minute idle
        expiry. No raw session secret in JSON.
    APIKey:
      type: http
      scheme: bearer
      description: Future organization/resource-scoped machine key. Not implemented.
  parameters:
    RequestID:
      name: X-Request-ID
      in: header
      required: false
      description: Valid bounded IDs are propagated; missing/invalid values are replaced.
      schema:
        type: string
        pattern: ^[A-Za-z0-9_-]{1,64}$
    CSRF:
      in: header
      name: X-CSRF-Token
      required: true
      schema:
        type: string
        minLength: 43
        maxLength: 43
      description: Must match the host-only waba_csrf cookie; authenticated actions also verify its session-stored digest.
    Origin:
      in: header
      name: Origin
      required: true
      schema:
        type: string
      description: Exact configured PUBLIC_ORIGIN. Cross-origin state changes are rejected.
    Revision:
      in: header
      name: If-Match
      required: true
      schema:
        type: string
        pattern: ^"[1-9][0-9]*"$
      description: Current resource revision; missing returns 428, stale returns 409.
  responses:
    Health:
      description: Check passed.
      headers:
        X-Request-ID:
          schema:
            type: string
          description: Server correlation ID.
      content:
        application/json:
          schema:
            type: object
            additionalProperties: false
            required:
              - status
            properties:
              status:
                type: string
                enum:
                  - ok
                  - ready
    Error:
      description: Safe error with no underlying credentials or dependency details.
      headers:
        X-Request-ID:
          schema:
            type: string
          description: Server correlation ID.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
  schemas:
    BillingCurrencyEvidenceInput: {"type": "object", "properties": {"waba_id": {"type": "string", "format": "uuid"}, "currency": {"const": "IDR"}, "source": {"const": "OPERATOR_REVIEWED_META_BILLING"}, "reviewed_at": {"type": "string", "format": "date-time"}}, "required": ["waba_id", "currency", "source", "reviewed_at"], "additionalProperties": false}
    BillingCurrencyEvidenceResult: {"type": "object", "properties": {"billing_currency_state": {"const": "VERIFIED"}, "currency": {"const": "IDR"}, "evidence_digest": {"type": "string"}, "paid_authority": {"const": "CLOSED"}}, "required": ["billing_currency_state", "currency", "evidence_digest", "paid_authority"], "additionalProperties": false}
    ServiceExposureInput: {"type": "object", "properties": {"phone_id": {"type": "string", "format": "uuid"}, "market": {"const": "ID"}}, "required": ["phone_id", "market"], "additionalProperties": false}
    ServiceExposureEstimate: {"type": "object", "properties": {"currency": {"type": "string"}, "currency_evidence_digest": {"type": "string"}, "market": {"type": "string"}, "delivery_interval_start": {"type": "string", "format": "date-time"}, "delivery_interval_end": {"type": "string", "format": "date-time"}, "maximum": {"type": "string", "pattern": "^[0-9]{1,16}(\\.[0-9]{1,8})?$"}, "publication_ids": {"type": "array", "items": {"type": "string", "format": "uuid"}}, "evidence_digest": {"type": "string"}, "evidence_kind": {"const": "INTERNAL_ESTIMATE"}, "allowance_applied": {"const": false}}, "required": ["currency", "market", "delivery_interval_start", "delivery_interval_end", "maximum", "publication_ids", "evidence_kind", "allowance_applied"], "additionalProperties": false}
    TestBudgetProposal: {"type": "object", "properties": {"maximum": {"type": "string", "pattern": "^[0-9]{1,16}(\\.[0-9]{1,8})?$"}, "currency": {"const": "IDR"}, "state": {"const": "PENDING_OWNER_APPROVAL"}, "message_count": {"const": 1}, "message_type": {"const": "TEXT"}, "category": {"const": "SERVICE"}, "phone_id": {"type": "string", "format": "uuid"}, "recipient_scope": {"const": "CONFIGURED_TEST_ONLY"}}, "required": ["maximum", "currency", "state", "message_count", "message_type", "category", "phone_id", "recipient_scope"], "additionalProperties": false}
    ServiceExposurePreflight: {"type": "object", "properties": {"pricing_coverage": {"const": "COMPLETE"}, "exposure": {"$ref": "#/components/schemas/ServiceExposureEstimate"}, "server_now": {"type": "string", "format": "date-time"}, "quote_expires_at": {"type": "string", "format": "date-time"}, "authorization_expires_at": {"type": "null"}, "dispatch_allowed": {"const": false}, "paid_authority": {"const": "CLOSED"}, "owner_approval_required": {"const": true}, "budget_proposal": {"$ref": "#/components/schemas/TestBudgetProposal"}}, "required": ["pricing_coverage", "exposure", "server_now", "quote_expires_at", "authorization_expires_at", "dispatch_allowed", "paid_authority", "owner_approval_required", "budget_proposal"], "additionalProperties": false}
    InboxCommand: {"type": "object", "properties": {"saved": {"type": "boolean"}, "id": {"type": "string", "format": "uuid"}, "state": {"type": "string"}, "reused": {"type": "boolean"}}, "additionalProperties": false}
    InboxConversation: {"type": "object", "properties": {"id": {"type": "string"}, "phone_id": {"type": "string"}, "display_name": {"type": "string"}, "identity_value": {"type": "string"}, "sending_phone": {"type": "string"}, "status": {"type": "string"}, "priority": {"type": "string"}, "handoff_state": {"type": "string"}, "window_confidence": {"type": "string"}, "assigned_team_id": {"type": ["string", "null"], "format": "uuid"}, "assigned_member_id": {"type": ["string", "null"], "format": "uuid"}, "revision": {"type": "integer", "format": "int64", "minimum": 0}, "assignment_revision": {"type": "integer", "format": "int64", "minimum": 0}, "inbound_sequence": {"type": "integer", "format": "int64", "minimum": 0}, "unread_count": {"type": "integer", "format": "int64", "minimum": 0}, "manual_unread": {"type": "boolean"}, "window_expires_at": {"type": ["string", "null"], "format": "date-time"}, "server_now": {"type": "string", "format": "date-time"}}}
    InboxMessage: {"type": "object", "properties": {"id": {"type": "string", "format": "uuid"}, "provider_id": {"type": ["string", "null"]}, "direction": {"type": "string", "enum": ["INBOUND", "OUTBOUND"]}, "message_type": {"type": "string", "enum": ["TEXT", "IMAGE", "VIDEO", "AUDIO", "DOCUMENT", "STICKER", "CONTACTS", "LOCATION", "REACTION", "INTERACTIVE", "TEMPLATE", "UNKNOWN"]}, "content": {"type": "object", "additionalProperties": true}, "text_body": {"type": "string"}, "provider_at": {"type": ["string", "null"], "format": "date-time"}, "received_at": {"type": "string", "format": "date-time"}, "inbound_sequence": {"type": ["integer", "null"]}, "delivery_state": {"type": "string", "enum": ["RECEIVED", "PENDING_LOCAL", "ACCEPTED", "SENT", "DELIVERED", "READ", "FAILED", "UNCERTAIN", "UNKNOWN_CONFLICT"]}, "processing_state": {"type": "string"}, "error_code": {"type": ["string", "null"]}, "revision": {"type": "integer", "format": "int64", "minimum": 0}}}
    InternalNote: {"type": "object", "properties": {"id": {"type": "string", "format": "uuid"}, "author_member_id": {"type": "string", "format": "uuid"}, "body": {"type": "string", "maxLength": 8000}, "revision": {"type": "integer", "format": "int64", "minimum": 0}, "created_at": {"type": "string", "format": "date-time"}, "updated_at": {"type": "string", "format": "date-time"}, "redacted_at": {"type": ["string", "null"], "format": "date-time"}, "mentioned_me": {"type": "boolean"}}}
    OutboundIntent: {"type": "object", "properties": {"id": {"type": "string", "format": "uuid"}, "conversation_id": {"type": "string", "format": "uuid"}, "state": {"type": "string", "enum": ["QUEUED", "RESERVED", "DISPATCHING", "ACCEPTED", "BLOCKED", "FAILED", "UNCERTAIN"]}, "error_code": {"type": ["string", "null"]}, "provider_id": {"type": ["string", "null"]}, "created_at": {"type": "string", "format": "date-time"}, "finished_at": {"type": ["string", "null"], "format": "date-time"}}}
    SendDecision: {"type": "object", "properties": {"allowed": {"type": "boolean"}, "code": {"type": "string", "description": "Stable domain result. PRICING_HORIZON_NOT_FULLY_COVERED means executable evidence does not cover the full possible delivery interval, even though the ordinary Service TTL is known (30 days)."}, "window": {"type": "string", "enum": ["ACTIVE", "EXPIRING_SOON", "EXPIRED", "UNKNOWN"]}, "category": {"type": "string"}, "pricing": {"type": "string"}, "billing_currency_state": {"type": "string", "enum": ["UNKNOWN", "VERIFIED"]}, "confirmation_required": {"type": "boolean"}, "financial_reservation_required": {"type": "boolean"}}, "required": ["allowed", "code"], "additionalProperties": false}
    SendPreflight: {"type": "object", "properties": {"decision": {"$ref": "#/components/schemas/SendDecision"}, "authorization": {"type": "string", "minLength": 43, "maxLength": 43, "description": "Opaque single-use token; keep only in memory. Bound to actor/session/org/recipient/phone/text/assignment/policy."}, "expires_at": {"type": "string", "format": "date-time"}, "server_now": {"type": "string", "format": "date-time"}, "policy_id": {"type": "string", "format": "uuid"}, "window_expires_at": {"type": ["string", "null"], "format": "date-time"}, "policy_basis": {"type": "string"}, "paid_authority": {"type": "string", "enum": ["CLOSED"]}, "service_delivery_ttl_seconds": {"type": "integer", "const": 2592000, "description": "M73 ordinary Service TTL. Does not establish complete pricing coverage or dispatch authority."}}, "required": ["decision"], "additionalProperties": false}
    PreflightInput: {"type": "object", "properties": {"conversation_id": {"type": "string", "format": "uuid"}, "assignment_revision": {"type": "integer", "format": "int64", "minimum": 0}, "text": {"type": "string", "minLength": 1, "maxLength": 4096}, "message_type": {"type": "string", "default": "TEXT"}, "category": {"type": "string", "default": "SERVICE"}}, "required": ["conversation_id", "assignment_revision", "text"], "additionalProperties": false}
    SubmitIntentInput: {"type": "object", "properties": {"conversation_id": {"type": "string", "format": "uuid"}, "assignment_revision": {"type": "integer", "format": "int64", "minimum": 0}, "text": {"type": "string", "minLength": 1, "maxLength": 4096}, "message_type": {"type": "string", "default": "TEXT"}, "category": {"type": "string", "default": "SERVICE"}, "client_idempotency_key": {"type": "string", "minLength": 16, "maxLength": 128}, "authorization": {"type": "string", "minLength": 43, "maxLength": 43}}, "required": ["conversation_id", "assignment_revision", "text", "client_idempotency_key", "authorization"], "additionalProperties": false}
    ConversationPatch: {"type": "object", "properties": {"revision": {"type": "integer", "format": "int64", "minimum": 0}, "status": {"type": "string", "enum": ["OPEN", "RESOLVED", "SNOOZED"]}, "priority": {"type": "string", "enum": ["LOW", "NORMAL", "HIGH", "URGENT"]}, "handoff_state": {"type": "string", "enum": ["HUMAN", "WAITING_AGENT"]}, "snoozed_until": {"type": "string", "format": "date-time"}}, "required": ["revision"], "additionalProperties": false}
    AssignmentInput: {"type": "object", "properties": {"assignment_revision": {"type": "integer", "format": "int64", "minimum": 0}, "team_id": {"type": ["string", "null"], "format": "uuid"}, "member_id": {"type": ["string", "null"], "format": "uuid"}}, "required": ["assignment_revision"], "additionalProperties": false}
    ReadInput: {"type": "object", "properties": {"watermark": {"type": "integer", "format": "int64", "minimum": 0}, "manual_unread": {"type": "boolean"}}, "required": ["watermark"], "additionalProperties": false}
    NoteInput: {"type": "object", "properties": {"body": {"type": "string", "minLength": 1, "maxLength": 8000}, "mentions": {"type": "array", "items": {"type": "string", "format": "uuid"}}}, "required": ["body"], "additionalProperties": false}
    NoteEdit: {"type": "object", "properties": {"revision": {"type": "integer", "format": "int64", "minimum": 0}, "body": {"type": "string", "minLength": 1, "maxLength": 8000}}, "required": ["revision", "body"], "additionalProperties": false}
    RevisionInput: {"type": "object", "properties": {"revision": {"type": "integer", "format": "int64", "minimum": 0}}, "required": ["revision"], "additionalProperties": false}
    RegistryActionInput: {"type": "object", "properties": {"revision": {"type": "integer", "format": "int64", "minimum": 0}, "approve": {"type": "boolean"}}, "required": ["revision"], "additionalProperties": false}
    RateImportInput: {"type": "object", "properties": {"source": {"type": "string", "enum": ["GATE_C_20260914", "SPRINT3_IDR_SERVICE_20260918"]}, "correction_reason": {"type": "string", "maxLength": 1000, "description": "Required for a correction when an active publication exists; retained with the reviewed import."}}, "required": ["source"], "additionalProperties": false}
    SendingSettingsInput: {"type": "object", "properties": {"sending_enabled": {"type": "boolean"}, "revision": {"type": "integer", "format": "int64", "minimum": 0}}, "required": ["sending_enabled", "revision"], "additionalProperties": false}
    BudgetInput: {"type": "object", "properties": {"Name": {"type": "string", "minLength": 1, "maxLength": 80}, "Currency": {"type": "string", "pattern": "^[A-Z]{3}$"}, "Maximum": {"type": "string", "pattern": "^[0-9]{1,16}(\\.[0-9]{1,8})?$"}, "Starts": {"type": "string", "format": "date-time"}, "Ends": {"type": "string", "format": "date-time"}}, "required": ["Name", "Currency", "Maximum", "Starts", "Ends"], "additionalProperties": false}
    InboxSetup: {"type": "object", "properties": {"member_id": {"type": "string", "format": "uuid"}, "session_id": {"type": "string", "format": "uuid"}, "settings": {"type": "array", "items": {"type": "object", "properties": {"sending_enabled": {"type": "boolean"}, "billing_currency_state": {"type": "string"}, "billing_currency": {"type": ["string", "null"]}, "policy_revision": {"type": "integer", "format": "int64", "minimum": 0}}, "additionalProperties": false}}, "teams": {"type": "array", "items": {"type": "object", "properties": {"id": {"type": "string", "format": "uuid"}, "name": {"type": "string"}}, "additionalProperties": false}}, "members": {"type": "array", "items": {"type": "object", "properties": {"id": {"type": "string", "format": "uuid"}, "name": {"type": "string"}, "team_id": {"type": ["string", "null"], "format": "uuid"}}, "additionalProperties": false}}, "live_test_configured": {"type": "boolean"}, "paid_authority": {"type": "string", "enum": ["CLOSED"]}, "template_send": {"type": "string", "enum": ["DISABLED"]}}, "additionalProperties": false}
    ConversationPage: {"type": "object", "properties": {"items": {"type": "array", "items": {"$ref": "#/components/schemas/InboxConversation"}}, "has_more": {"type": "boolean"}, "next_cursor": {"type": ["string", "null"], "format": "uuid"}}, "required": ["items", "has_more", "next_cursor"], "additionalProperties": false}
    InboxThread: {"type": "object", "properties": {"conversation": {"type": "array", "items": {"$ref": "#/components/schemas/InboxConversation"}}, "messages": {"type": "array", "items": {"$ref": "#/components/schemas/InboxMessage"}}, "notes": {"type": "array", "items": {"$ref": "#/components/schemas/InternalNote"}}, "presence": {"type": "array", "items": {"type": "object", "properties": {"member_id": {"type": "string", "format": "uuid"}, "name": {"type": "string"}, "expires_at": {"type": "string", "format": "date-time"}}, "additionalProperties": false}}, "permissions": {"type": "array", "items": {"type": "string"}}, "message_has_more": {"type": "boolean"}, "message_next_cursor": {"type": ["string", "null"], "format": "uuid"}}, "additionalProperties": false}
    PricingRegistry: {"type": "object", "properties": {"imports": {"type": "array", "items": {"type": "object", "properties": {"id": {"type": "string", "format": "uuid"}, "state": {"type": "string", "enum": ["DRAFT", "VALIDATED", "DIFFED", "IN_REVIEW", "CHANGES_REQUIRED", "APPROVED", "PUBLISHED"]}, "revision": {"type": "integer", "format": "int64", "minimum": 0}, "source_url": {"type": "string", "format": "uri"}, "source_sha256": {"type": "string"}, "retrieved_at": {"type": "string", "format": "date-time"}, "import_hash": {"type": "string"}, "validation_report": {"type": ["object", "null"]}, "diff_report": {"type": ["object", "null"]}, "base_publication_id": {"type": ["string", "null"], "format": "uuid"}, "reviewer_member_id": {"type": ["string", "null"], "format": "uuid"}, "next_review_at": {"type": "string", "format": "date-time"}, "correction_reason": {"type": "string"}}}}, "publications": {"type": "array", "items": {"type": "object", "properties": {"id": {"type": "string", "format": "uuid"}, "snapshot_hash": {"type": "string"}, "source_sha256": {"type": "string"}, "published_at": {"type": "string", "format": "date-time"}, "next_review_at": {"type": "string", "format": "date-time"}, "supersedes_publication_id": {"type": ["string", "null"], "format": "uuid"}}}}, "policies": {"type": "array", "items": {"type": "object", "properties": {"id": {"type": "string", "format": "uuid"}, "version": {"type": "string"}, "kind": {"type": "string"}, "effective_from": {"type": "string", "format": "date-time"}, "effective_to": {"type": "string", "format": "date-time"}, "next_review_at": {"type": "string", "format": "date-time"}, "source_url": {"type": "string", "format": "uri"}, "source_sha256": {"type": "string"}, "review_evidence": {"type": "string"}}}}, "settings": {"type": "array", "items": {"type": "object", "properties": {"billing_currency_state": {"type": "string"}, "billing_currency": {"type": ["string", "null"]}, "active_rate_publication_id": {"type": ["string", "null"], "format": "uuid"}}, "additionalProperties": false}}, "paid_authority": {"type": "string", "enum": ["CLOSED"]}}, "additionalProperties": false}
    BudgetExposure: {"type": "object", "properties": {"id": {"type": "string", "format": "uuid"}, "name": {"type": "string"}, "currency": {"type": "string"}, "starts_at": {"type": "string", "format": "date-time"}, "ends_at": {"type": "string", "format": "date-time"}, "hard_limit": {"type": "string"}, "exposure": {"type": "string"}, "revision": {"type": "integer", "format": "int64", "minimum": 0}}, "additionalProperties": false}
    EmptyInput: {"type": "object", "properties": {}, "additionalProperties": false}
    MetaConnection:
      type: object
      required: [items, has_more, next_cursor]
      properties:
        has_more:
          type: boolean
        next_cursor:
          type: [string, 'null']
          format: uuid
        items:
          type: array
          maxItems: 100
          items:
            type: object
            additionalProperties: false
            required: [id, external_id, callback_key, configured_waba_id, graph_version, credential_state, last_sync_at, last_error, created_at]
            properties:
              id:
                type: string
                format: uuid
              external_id:
                type: string
              callback_key:
                type: string
                format: uuid
              configured_waba_id:
                type: string
              graph_version:
                type: string
              credential_state:
                type: string
              last_sync_at:
                type: [string, 'null']
                format: date-time
              last_error:
                type: [string, 'null']
              created_at:
                type: string
                format: date-time
    MetaSync:
      type: object
      required: [items, has_more, next_cursor]
      properties:
        has_more:
          type: boolean
        next_cursor:
          type: [string, 'null']
          format: uuid
        items:
          type: array
          maxItems: 100
          items:
            type: object
            additionalProperties: false
            required: [id, app_id, state, attempt_count, error_code, graph_version, created_at, finished_at]
            properties:
              id:
                type: string
                format: uuid
              app_id:
                type: string
                format: uuid
              state:
                type: string
              attempt_count:
                type: integer
              error_code:
                type: [string, 'null']
              graph_version:
                type: string
              created_at:
                type: string
                format: date-time
              finished_at:
                type: [string, 'null']
                format: date-time
    MetaWABA:
      type: object
      required: [items, has_more, next_cursor]
      properties:
        has_more:
          type: boolean
        next_cursor:
          type: [string, 'null']
          format: uuid
        items:
          type: array
          maxItems: 100
          items:
            type: object
            additionalProperties: false
            required: [id, external_id, name, timezone_id, subscribed, lifecycle, graph_version, synced_at]
            properties:
              id:
                type: string
                format: uuid
              external_id:
                type: string
              name:
                type: string
              timezone_id:
                type: string
              subscribed:
                type: boolean
              lifecycle:
                type: string
              graph_version:
                type: string
              synced_at:
                type: string
                format: date-time
    MetaPhone:
      type: object
      required: [items, has_more, next_cursor]
      properties:
        has_more:
          type: boolean
        next_cursor:
          type: [string, 'null']
          format: uuid
        items:
          type: array
          maxItems: 100
          items:
            type: object
            additionalProperties: false
            required: [id, waba_id, external_id, name, display_number, quality, platform, code_verification_status, lifecycle, graph_version, synced_at]
            properties:
              id:
                type: string
                format: uuid
              waba_id:
                type: string
                format: uuid
              external_id:
                type: string
              name:
                type: string
              display_number:
                type: string
              quality:
                type: string
              platform:
                type: string
              code_verification_status:
                type: string
              lifecycle:
                type: string
              graph_version:
                type: string
              synced_at:
                type: string
                format: date-time
    MetaProfile:
      type: object
      required: [items, has_more, next_cursor]
      properties:
        has_more:
          type: boolean
        next_cursor:
          type: [string, 'null']
          format: uuid
        items:
          type: array
          maxItems: 100
          items:
            type: object
            additionalProperties: false
            required: [id, phone_id, fields, graph_version, synced_at]
            properties:
              id:
                type: string
                format: uuid
              phone_id:
                type: string
                format: uuid
              fields:
                type: object
              graph_version:
                type: string
              synced_at:
                type: string
                format: date-time
    MetaEvent:
      type: object
      required: [items, has_more, next_cursor]
      properties:
        has_more:
          type: boolean
        next_cursor:
          type: [string, 'null']
          format: uuid
        items:
          type: array
          maxItems: 100
          items:
            type: object
            additionalProperties: false
            required: [id, app_id, received_at, state, event_class, attempt_count, last_error, request_id, parser_version, processed_at]
            properties:
              id:
                type: string
                format: uuid
              app_id:
                type: string
                format: uuid
              received_at:
                type: string
                format: date-time
              state:
                type: string
              event_class:
                type: string
              attempt_count:
                type: integer
              last_error:
                type: [string, 'null']
              request_id:
                type: string
              parser_version:
                type: integer
              processed_at:
                type: [string, 'null']
                format: date-time
    MetaCommand:
      type: object
      properties:
        id:
          type: string
          format: uuid
        queued:
          type: boolean
        bound:
          type: boolean
        generation:
          type: integer
    MetaHealth:
      type: object
      required: [connections, processing, runtime_configured, scope]
      properties:
        runtime_configured:
          type: boolean
        scope:
          type: string
        connections:
          type: array
          items:
            type: object
            description: App ID, Graph version, credential state, last sync/error, valid challenge time and last authentic webhook time. Provider provenance requires separate live acceptance evidence.
        processing:
          type: array
          items:
            type: object
            properties:
              state:
                type: string
              count:
                type: integer
    MetaDetail:
      type: object
      required: [id, state, generation, payload_retained, attempts, facts]
      properties:
        id:
          type: string
          format: uuid
        state:
          type: string
        generation:
          type: integer
        payload_retained:
          type: boolean
        attempts:
          type: array
          maxItems: 100
          items:
            type: object
            description: generation, attempt_no, result, error_code, parser_version, created_at.
        facts:
          type: array
          maxItems: 100
          items:
            type: object
            description: event_class, internal waba_id and phone_id, uncertain_identity. No message body or customer identity.
    MetaPayload:
      type: object
      description: Structural inspection only. Arbitrary keys, provider strings, contacts, IDs and content are withheld; quarantined and expired evidence is denied. Inspection is audited.
    ErrorEnvelope:
      type: object
      additionalProperties: false
      required:
        - error
      properties:
        error:
          type: object
          additionalProperties: false
          required:
            - code
            - message
            - request_id
          properties:
            code:
              type: string
            message:
              type: string
            request_id:
              type: string
            details:
              type: object
              additionalProperties: true
            field_errors:
              type: array
              items:
                type: object
                required:
                  - field
                  - code
                properties:
                  field:
                    type: string
                  code:
                    type: string
    IdentityInput:
      type: object
      additionalProperties: false
      properties:
        email:
          type: string
          format: email
          maxLength: 254
        password:
          type: string
          minLength: 12
          maxLength: 256
          writeOnly: true
        code:
          type: string
          writeOnly: true
        token:
          type: string
          writeOnly: true
        name:
          type: string
          maxLength: 100
        organization_id:
          type: string
          format: uuid
        member_id:
          type: string
          format: uuid
        role_ids:
          type: array
          maxItems: 20
          items:
            type: string
            format: uuid
        permissions:
          type: array
          maxItems: 30
          items:
            type: string
            enum:
              - organization.view
              - members.view
              - members.invite
              - members.manage
              - teams.view
              - teams.manage
              - roles.view
              - roles.manage
              - roles.assign
              - owners.manage
              - audit.view
        scope:
          type: string
          enum:
            - ORG
            - TEAM
            - SELF
        team_id:
          anyOf:
            - type: string
              format: uuid
            - type: 'null'
        grants:
          type: array
          maxItems: 20
          items:
            type: object
            additionalProperties: false
            required:
              - role_id
              - scope
              - team_id
            properties:
              role_id:
                type: string
                format: uuid
              scope:
                type: string
                enum:
                  - ORG
                  - TEAM
                  - SELF
              team_id:
                type:
                  - string
                  - 'null'
                format: uuid
    IdentityResult:
      type: object
      description: >-
        Action-specific response. Opaque session tokens are emitted only in HttpOnly cookies. Access changes invalidate
        affected organization sessions; the acting session rotates when it retains membership. Organization switching
        preserves the original absolute expiry and reauthentication time.
      properties:
        ok:
          type: boolean
        authenticated:
          type: boolean
        csrf_token:
          type: string
        mfa_required:
          type: boolean
        challenge:
          type: string
          description: One-use MFA login proof, five-minute expiry.
        id:
          type: string
          format: uuid
        revision:
          type: integer
        state:
          type: string
        organization_id:
          type: string
          format: uuid
        secret:
          type: string
          description: TOTP seed returned only by enrollment.
        uri:
          type: string
        recovery_codes:
          type: array
          items:
            type: string
          description: Returned only during enrollment/regeneration; never stored cleartext.
        user:
          type: object
          properties:
            id:
              type: string
              format: uuid
            user_id:
              type: string
              format: uuid
            organization_id:
              anyOf:
                - type: string
                  format: uuid
                - type: 'null'
            email:
              type: string
              format: email
            name:
              type: string
            verified:
              type: boolean
            mfa_enabled:
              type: boolean
        organizations:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
                format: uuid
              name:
                type: string
              member_id:
                type: string
                format: uuid
        name:
          type: string
        timezone:
          type: string
        member_id:
          type: string
          format: uuid
        permissions:
          type: array
          items:
            type: string
        result:
          type: object
        scoped_teams:
          type: array
          items:
            type: object
            required:
              - id
              - permissions
            properties:
              id:
                type: string
                format: uuid
              permissions:
                type: array
                items:
                  type: string
    IdentityList:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            type: object
            description: >-
              Resource rows contain only the requested organization's records or the authenticated user's sessions; no
              credential columns.
        next_cursor:
          type:
            - string
            - 'null'
        has_more:
          type: boolean
      description: >-
        At most 100 records per page. Follow next_cursor until null; permission and tenant scope are reevaluated on
        every request.
